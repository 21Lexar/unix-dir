#ifndef TREE_H
#define TREE_H

#include "struct.h"


int insert_file(directory *parent_directory, file *file_to_insert){
        if(file_to_insert == NULL){
                return -1;
        }
        
        
}

#endif